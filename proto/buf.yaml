version: v1
name: buf.build/xmtp/example-notification-server-go
breaking:
  use:
    - FILE
lint:
  use:
    - STANDARD
  # These established public names predate the current standard rules.
  # Renaming the service or replacing Empty responses would break clients.
  except:
    - SERVICE_SUFFIX
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
